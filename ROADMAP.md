# Roadmap — Ecole (Layaida)

> Executor contract: each night an unattended Sonnet agent (`claude -p`, cwd = this repo) picks the FIRST unchecked task, does ONLY that task, verifies it per the task's Verify note, commits with an `autoloop:` prefix (one logical change per commit, never leave the tree dirty), then ticks the checkbox adding the date and a one-line result, and appends details to ## Log. If verification fails: revert, leave unchecked, add a `BLOCKED:` note. Tasks marked **NEEDS-SAMY** are decisions/credentials only Samy can provide — never attempt them unattended.

## Context for the executor

- **Stack:** Next.js 15 App Router + React 19 + Tailwind CSS 4 + TypeScript + lucide-react. Verification gate: `npm run typecheck && npm run build` (both pass — keep them passing). No test suite yet (see P1-T6).
- **State (2026-07-08):** the full « Nord Campus » redesign is implemented (see `design/handoff/README.md` — the spec; `DESIGN_PROMPT.md` — the original brief). All screens exist: accueil, auth, onboarding, dashboard, catalogue `/courses`, fiche cours, lecteur, quiz, mes cours, profil, teacher, parent, 404/erreur/offline. Light+dark themes, fr/en/ar with full RTL.
- **Data model:** still intentionally **mock/local** — courses in `src/lib/data.ts` (trilingual), auth hardcoded in `src/app/api/auth/*`, all learner state per-user in localStorage (`src/lib/progress.ts`). Phase 1 stays mock; Phases 2+ replace it deliberately, task by task.
- **Trilingual rule (never break):** every user-visible string goes through `src/lib/i18n.ts` in all three locales; the `TranslationKeys` type enforces it.
- **RTL rule:** logical Tailwind utilities only (`ms-/me-/ps-/pe-/start-/end-/text-start/text-end`); directional icons must mirror (`dir === "rtl"` → `-scale-x-100`).
- **Design rules:** tokens only (no raw hex in components); type scale 30/22/17/15/13 + mono 11; motion 180ms ease-out; touch targets ≥44px; empty states always suggest an action.
- Never add a dependency unattended — propose as NEEDS-SAMY.

## Phase 1 — Polish & content depth (mock data, autoloop-safe)

Goal: make the mock platform indistinguishable from a real one in look and feel.

- [x] **P1-T1 — Quiz coverage for every chapter** (M) (2026-07-08: added 3 trilingual QCM questions each to all 24 previously-quizless chapters across the 10 courses; every chapter now has a quiz matching the existing shape).
- [x] **P1-T2 — Real SVG course thumbnails** (M) (2026-07-09: added one geometric, subject-tinted inline SVG per course in `public/thumbnails/` (11 files matching all 11 course ids), CourseCard now renders it via `next/image` with the subject tint kept as the loading background; enabled `images.dangerouslyAllowSVG` with a locked-down CSP in next.config.ts since these are locally-authored assets).
- [x] **P1-T3 — Per-course outcomes + reviews** (M) (2026-07-09: wrote 4 trilingual outcomes + 2 reviews each for the 10 courses that previously fell back to `defaultOutcomes`/empty reviews — geometry, statistics, trigonometry, mechanics, electricity, optics, cells, ecology, human-body, genetics; math-algebra-101 already had custom content. A script confirms all 11 courses now have 4 outcomes and ≥2 reviews via `getCourseExtras`).
- [x] **P1-T4 — Skeleton loading states** (S) (2026-07-09: added `loading.tsx` for /courses, /dashboard, /my-courses using `Skeleton`/`CourseCardSkeleton`, each mirroring its route's real layout).
- [x] **P1-T5 — Route metadata & SEO** (S) (2026-07-09: added a thin server `layout.tsx` per route exporting metadata — static title/description for courses, dashboard, my-courses, onboarding, parent, profile, signin, signup; `generateMetadata` reading `getCourse`/`getTeacher` from `data.ts` for `course/[courseId]` and `teacher/[slug]`. Root layout gained an `openGraph.images` placeholder pointing at `logo.png`. `npm run typecheck && npm run build` pass; curl against a local prod server confirmed a distinct `<title>` per route).
- [ ] **P1-T6 — Smoke-test suite** (M) — no tests exist. Propose (NEEDS-SAMY approval for the dependency) Playwright or vitest+testing-library; once approved, cover: signup→onboarding→enroll→complete-lesson→quiz happy path, locale switch flips `dir`, dark-mode toggle. Until approved: BLOCKED.
- [x] **P1-T7 — Dark-mode contrast audit** (S) (2026-07-09: computed WCAG contrast for every text-on-tint pair used in `.dark`; `.dark` only remapped neutrals/primary/subject-soft, leaving `--color-success/-warning/-error` + their `-soft` tints and the subject solids at light-mode values on dark surfaces — fixed by adding dark-specific values, same treatment as the existing primary lighten/soften pattern. warning-on-warning-soft 2.51→7.32, error-on-error-soft 3.68→5.79, success-on-success-soft 2.84→6.56, math/physics/biology-on-soft-badge 3.28-3.42→5.38-6.56, primary-hover-on-primary-soft chip (course lesson-row numbers) 3.85→5.04 by dropping a weaker `dark:text-primary` override. Left `text-faint` alone — it already fails AA in light mode too (2.81-3.06), so it's a pre-existing cross-theme issue, not dark-specific; flagging for a future task rather than re-theming light mode here).
- [x] **P1-T8 — Arabic copy review** (S) (2026-07-09: swept `i18n.ts` + `data.ts` for tone/calque/terminology issues; the corpus was already high-quality — consistent informal register, consistent دروس/حصص pattern — so fixed only the 6 genuine issues found: `i18n.ts` auth.welcome diacritic (مرحبا→مرحباً); `data.ts` biology-ecology-201 and biology-genetics-301 course titles (dropped redundant/ambiguous second noun: علم البيئة والمحيط→علم البيئة, علم الوراثة والتوارث→علم الوراثة); a physics lesson description's non-standard "انحفاظ" coinage→"حفظ" (conservation); the physics teacher's bio (×3 occurrences) made a full sentence to match the math teacher's bio pattern; a genetics quiz option's awkward idafa construction fixed. Wordmark العيايدة untouched. NEEDS-SAMY final native read still recommended).
  - SAMY 2026-07-12: deferred — Needs a native Arabic reader before sign-off
- [x] **P1-T9 — Streak/goal edge cases** (S) (2026-07-09: `recordActiveDay()` now also fires from the lesson-player's per-lesson-load effect (guarded on a signed-in user), so visiting a lesson without finishing it keeps the streak alive, not just completion/quiz. `getWeeklyActivity` now caps each day's count at 9 (`Math.min(counts[idx] + 1, 9)`) so the dashboard bar chart can't be skewed by an unrealistic single-day count. `npm run typecheck && npm run build` pass).
- [x] **P1-T10 — Motion foundation + press feedback everywhere** (S) (2026-07-10: added `--ease-out-custom/--ease-in-out-custom/--ease-drawer` + `--duration-fast/base/slow` to globals.css (180ms formalized as `--duration-base`; `prefers-reduced-motion` block already existed from the redesign); replaced all 13 `transition-all` sites with property-specific `transition-[...]` lists; extended `active:scale-[0.98]` press feedback to Navbar, Tabs, CourseCard, quiz options, onboarding/signup selection cards, courses filter chips, and course-detail/lesson-player navigation rows that lacked it; converted the layout-property violations the sweep turned up — two toggle-switch knobs (profile/onboarding) from animated `inset-inline-start` to `transform: translateX` (RTL-aware sign via `dir`), the dashboard weekly-activity bars from animated inline `height` to `transform: scaleY` with `origin-bottom`, the skip-to-content link from animated `top` to `transform: translateY`, and 3 progress bars (shared `ProgressBar`, quiz progress header) from animated `width` to `transform: scaleX` per CLAUDE.md's explicit scaleX rule — to transform/opacity-only per the interaction-craft hard floor. `npm run typecheck && npm run build` pass (18/18 routes); `grep -rn 'transition-all' src` empty; vars confirmed present in the compiled CSS output).
- [x] **P1-T11 — Lesson/quiz celebration moments** (M) (2026-07-11: added a reusable `CelebrationCheck` SVG — circle scale/fade-in then checkmark stroke draw-in, ~300ms combined via new `celebration-circle`/`celebration-mark` keyframes in globals.css, transform/opacity/stroke-dashoffset only, killed by the existing reduced-motion block. Wired as a brief overlay on the lesson-complete flow — both the manual "mark complete" button and the 80%-watched auto-complete path — and on the quiz results screen when the final score is ≥60% (same threshold already used for the "good job" message/ring color). No new strings needed. `npm run typecheck && npm run build` pass, 18/18 routes; confirmed the compiled JS chunks for both routes and the compiled CSS contain the new celebration code — couldn't curl-verify the live DOM since both pages are client-auth-gated with spinner-only SSR, same pre-existing constraint noted in the P3-T4 log).
- [x] **P1-T12 — Navigation enter transitions (RTL-aware)** (M) (2026-07-11: added a `.page-enter` keyframe (translateY 4px + opacity, 200ms, `--ease-out-custom`, direction-neutral so no RTL flip needed) applied to the `<main>` of `/courses`, `course/[courseId]`, and the lesson page — the three route mains the task named. Added `.menu-enter` (scale 0.95→1 + opacity, 150ms via `--duration-fast`) to the Navbar language dropdown, the only dropdown/menu component in the codebase, with origin-aware corner `origin-top-right rtl:origin-top-left` matching its `end-0` anchoring, same `rtl:origin-*` pattern already used on the P1-T10 progress-bar scaleX conversions. CSS-only, no new deps.)
- [x] **P1-T13 — Quiz in the lesson flow + course-completion moment** (M) (2026-07-12: added a `computeNextHref` helper to the lesson page — on a chapter's last lesson it now routes next/auto-advance to that chapter's quiz if `getLastQuizAttempt` shows no ≥60% attempt yet, else skips to the next chapter's first lesson; the dead `<span/>` on the course's final lesson is replaced by a course-complete panel (`CelebrationCheck` + new trilingual `lesson.courseComplete{Title,Body}` keys + dashboard/my-courses CTAs). Quiz results gained a primary "Continuer" CTA (`quizNextHref`, reusing existing `t.common.continue`) to the next chapter's first lesson, falling back to the course page on the last chapter; passing the last chapter's quiz shows the same course-complete panel instead of the plain fallback).
- [ ] **P1-T14 — Overlay a11y: focus trap, restore, Escape, scroll lock; de-link locked rows** (M) — (audit #4/#5) `Modal` traps focus + restores it to the trigger on close + locks body scroll; the lesson chapters drawer and catalog filter sheet get the same four behaviors (Escape included — they have none today); Navbar language dropdown closes on outside click and Escape. Course-page locked lesson/quiz rows stop being `<Link href="#">` — render a non-interactive element with sr-only locked text (new trilingual key) alongside the Lock icon. Hand-rolled only, no new deps (focus trap is a small keydown handler over `focusable = querySelectorAll(...)`). Verify: typecheck + build; keyboard walk on a dev server: Tab cycles inside each open overlay, Escape closes, focus returns to trigger, locked rows unreachable by Tab.
- [ ] **P1-T15 — Keyboard patterns for tabs, segmented, quiz/onboarding radios** (S) — (audit #6) composite widgets announce ARIA roles without the matching keyboard behavior. Lesson tabs: roving tabindex + Arrow/Home/End keys + `role="tabpanel"` with `aria-labelledby` on the panes. `Segmented`: arrow keys; where it backs non-tab choices (locale/theme in /profile, level filter) switch to `radiogroup`/`radio` semantics. Quiz options and onboarding grade/goal groups: roving tabindex + arrow-key movement within the `radiogroup`. RTL: inline arrow keys follow reading direction (flip Left/Right handling under `dir === "rtl"`). Verify: typecheck + build; keyboard walk of a quiz question, the lesson tabs, and the profile locale switcher in fr + ar.
- [ ] **P1-T16 — Overlay & toast motion (the interaction-craft leftovers)** (S) — (audit #8; complements P1-T12 which owns page/dropdown enters) bottom sheets (lesson drawer, filter sheet) slide up with `--ease-drawer` ≤300ms + backdrop fade; `Modal` enters scale 0.97→1 + fade at `--duration-base`; toasts enter translateY(4px)+fade (CSS transition on mount, no keyframes needed); `ProgressRing`'s 500ms stroke animation drops to `--duration-slow` (300ms). Transform/opacity only, `transition-all` stays banned, reduced-motion inherits the global kill-switch. Verify: typecheck + build; grep confirms no new animated layout properties and no duration >300ms outside celebrations.
- [ ] **P1-T17 — `text-faint` AA contrast (both themes)** (S) — (audit #9; deliberately left during P1-T7 as a cross-theme issue) `--color-faint` sits at 2.81–3.06:1 on bg/surface in light mode and fails in dark too, but it's used for real text: lesson durations, sidebar timers, day labels, result counts. Either darken/lighten `--color-faint` per theme to ≥4.5:1 (compute with the P1-T7 relative-luminance script method, don't eyeball) or reclassify: text sites move to `text-muted`, `text-faint` stays for decorative glyphs only. Keep the change token-level or a mechanical class swap — no re-theming (Nord Campus is locked). Verify: typecheck + build; contrast script shows every text-carrying faint/muted pair ≥4.5:1 in light and dark.

## Phase 2 — Real accounts & data (NEEDS-SAMY decisions first)

Goal: multiple real users on one shared source of truth. Everything below changes infra — **queue, never unattended**.

- [x] **P2-T1 — pick the backend** — 2026-07-08, Samy: **SQLite on the homelab** (better-sqlite3, WAL, `data/layaida.db`, `LAYAIDA_DB` override). Done same day.
- [x] **P2-T2 — Real auth** — 2026-07-08: hand-rolled sessions instead of Auth.js (zero-dep MVP): scrypt hashes via node:crypto, httpOnly 30-day cookie, `/api/auth/{signup,login,logout,me,password}`, roles persisted. Email verification + CSRF hardening deferred to P4-T2.
- [x] **P2-T3 — Server-side learner state** — 2026-07-08: `learner_state` key/value mirror of the client namespace; `/api/state` GET/PUT; progress.ts debounce-pushes writes, pull-on-login (server wins), offline keeps local copy. Remaining polish: resync-on-reconnect event, conflict strategy beyond server-wins.
- [ ] **P2-T4 — Course content in the DB + admin CRUD** — courses/chapters/lessons/quizzes as tables; minimal admin screen (Samy-only) to create/edit content in the three languages. `data.ts` becomes the seed script.
- [ ] **P2-T5 — Parent↔child linking** — real parent accounts linked to a student (invite code at signup, per the design's child-linking flow); parent view reads the child's data server-side, read-only.

## Phase 3 — Real content & video

Goal: it teaches, not just demos. The YouTube placeholder embed must die.

- [x] **P3-T1 — video strategy** — 2026-07-08, Samy: **unlisted YouTube embeds** for the pilot; keep `videoUrl` a plain URL so Cloudflare Stream later is a data swap.
- [x] **P3-T2 — pilot curriculum: Algèbre (math-algebra-101)** — 2026-07-08, Samy picked Algèbre. NEEDS-SAMY: produce/collect the real lessons (videos as unlisted YouTube, PDFs, verified quizzes) — content production is on Samy; wiring is on the agents once URLs/files exist. *(2026-07-09: Samy declined to produce real content. Swapped the algebra course's 10 lesson `videoUrl`s from the site-wide rickroll placeholder to a distinct, clearly-commented illustrative placeholder (Big Buck Bunny, CC-BY) — every occurrence marked `// ILLUSTRATIVE placeholder ... pending real Algèbre video production` so it isn't mistaken for real curriculum later. Quizzes already exist per P1-T1 (not content-verified against real lessons, since lessons are still placeholder). PDFs remain `url: "#"` — that's P3-T3's scope, untouched here. `npm run typecheck && npm run build` pass, all 18 routes. Closing this as "as far as it goes without real content" rather than leaving it open indefinitely — reopen if Samy decides to produce real lessons later.)*
  - SAMY 2026-07-12: approved — Ship Algèbre pilot with clearly-marked placeholder video; real content later
- [x] **P3-T3 — Real documents** (2026-07-09: swapped all 29 `url: "#"` document links to a real served file, `public/documents/placeholder.pdf` — illustrative placeholder, same as P3-T2's video swap, since real per-course PDFs still need Samy to produce; added download tracking in `progress.ts` (`downloaded_docs`, synced via the existing `/api/state` key/value store, no schema change) with a "downloaded" checkmark in the lesson Documents tab; registered `public/sw.js`, a cache-first service worker scoped to `/documents/*`, so once a doc is downloaded it stays available offline per the design's promise. `npm run typecheck && npm run build` pass (18/18 routes); curled `/documents/placeholder.pdf` (200, `application/pdf`) and `/sw.js` (200, `application/javascript`) against a local prod server).
- [x] **P3-T4 — Video progress tracking** (2026-07-09: swapped the Date.now()-elapsed note timestamps for real playback position via the YouTube IFrame API — iframe now loads with `enablejsapi=1`, wired to a `YT.Player` that polls `getCurrentTime()`/`getDuration()` every 2s; position is persisted to a new `positions` key in `progress.ts` (synced via the existing `/api/state`) and restored with `seekTo` on player-ready for resume-at-position; a lesson auto-marks completed once ≥80% watched, reusing the existing `toggleLessonCompleted`. `npm run typecheck && npm run build` pass, 18/18 routes).

## Phase 4 — Launch ops

- [x] **P4-T1 — hosting + domain** — 2026-07-08, Samy: **homelab**, public at **ecole.samylayaida.com** via the existing CF tunnel. `ecole.service` runs on **127.0.0.1:3002** (unit in ~/infra/systemd/). ⚠️ Domain audit found ecole.samylayaida.com ALREADY serves a Vercel auto-deploy of this repo (broken auth there — serverless has no SQLite). DONE 2026-07-08: Samy unlinked Vercel + added the tunnel hostname; Secure cookies flipped on, public E2E green. Original steps were: (1) Vercel → Ecole project → remove domain + disconnect the repo or disable auto-deploy, (2) CF Zero Trust → Tunnels → homelab → Public Hostnames → add `ecole.samylayaida.com` → HTTP → `localhost:3002` (no Access policy — public app), (3) then remove `LAYAIDA_INSECURE_COOKIE=1` from ecole.service and restart (cookies become Secure behind CF TLS). NEEDS-SAMY: a scoped CF API token (Zone.DNS + Access/Tunnels edit) in ~/.config/homelab/secrets.env would let agents manage this henceforth.
  - SAMY 2026-07-12: deferred — P4-T1 already done and working; CF API token only needed when next tunnel/DNS change comes up
- [ ] **P4-T2 — Hardening** — rate-limit auth routes, security headers (CSP), zod validation at the API boundary, DB backups into the ~/backups rotation, error tracking.
- [ ] **P4-T3 — Monitoring & standing goal** — blackbox probe + Grafana panel like the other apps; graduate a standing goal in ~/infra/goals (predicate: prod URL 200 + build green) once live.
- [ ] **P4-T4 — Legal pages** — real Conditions/Confidentialité (footer links are `#`), cookie-less analytics or none, RGPD basics for minors' data (parental consent at signup).

## Phase 5 — Feel-real differentiators (post-launch)

- [ ] **P5-T1 — Certificates** — trilingual course-completion certificate (PDF) — the single strongest "real platform" signal for students/parents.
- [ ] **P5-T2 — Search that finds lessons** — index lesson titles/descriptions (not just courses) with highlighted matches; the hero search promises this.
- [ ] **P5-T3 — Spaced-repetition review** — a "Réviser" queue resurfacing failed quiz questions across chapters (wrongQuestionIds is already stored).
- [ ] **P5-T4 — Teacher Q&A** — per-lesson question thread (moderated) — planned as "later iteration" in the handoff.
- [ ] **P5-T5 — Exam-prep mode** — timed mock exams assembled from chapter quizzes per grade (brevet/bac alignment).
- [ ] **P5-T6 — Teacher dashboard** — upload/manage lessons (explicitly deferred in the handoff).

## Log

- 2026-07-11 (autoloop) — P1-T12 done. Skipped P1-T6 (still BLOCKED, unchanged NEEDS-SAMY dependency gate on a test framework) and P2-T4/P2-T5 (Phase 2 infra, forbidden unattended per the phase header), landing on the first unblocked unchecked task, P1-T12. Added two globals.css keyframe classes per the interaction-craft doctrine: `.page-enter` (translateY 4px + opacity, 200ms, `--ease-out-custom`) applied to the `<main>` of `/courses`, `course/[courseId]`, and the lesson page — translateY is direction-neutral so it satisfies the RTL requirement without a flip, same reasoning already used for the P1-T10 skip-link translateY conversion; `.menu-enter` (scale 0.95→1 + opacity, `--duration-fast`/150ms) applied to the Navbar language dropdown (the only dropdown/menu component present) with an origin-aware corner, `origin-top-right rtl:origin-top-left`, matching its existing `end-0` anchoring and following the `rtl:origin-*` pattern already established on the P1-T10 progress-bar scaleX conversions (confirmed via `grep -rn "rtl:" src` before writing this, rather than inventing a new pattern). `npm run typecheck && npm run build` pass (18/18 routes); grepped the compiled `.next/static/css` output and confirmed `.page-enter`/`.menu-enter` and both `origin-top-right`/`origin-top-left` utilities emit real rules, not just declared-and-unused. Could not curl-verify the animated DOM directly: `/courses`, `course/[courseId]`, and the lesson page are all client-auth-gated to a spinner-only SSR shell (confirmed by curling a local prod server against a scratch `LAYAIDA_DB=/tmp/ecole-smoke.db` — never touched the live DB), the same pre-existing constraint noted in the P3-T4 and P1-T11 log entries; relied on the compiled-asset check plus typecheck/build instead. Diff: 39 insertions / 4 deletions across 5 files, one commit.
- 2026-07-11 (attended, UX/UI audit) — Full-app audit against RTL/trilingual correctness, learner flows, interaction-craft, WCAG basics, responsive and states; written up in `docs/ux-audit-2026-07-11.md`. Fixed same-day (commits `2052f07`, `01aeab7`): progress bars filling from the wrong side under RTL (`origin-left` on the P1-T10 scaleX conversions → `rtl:origin-right`, compiled rule verified), the last unmirrored directional icon (quiz feedback ArrowUpRight), mixed Latin/Arabic-Indic chapter numbers (`padStart(2,"0")` → locale zero), skip-link mis-centering under RTL, and the English-only a11y strings (skip link ×8 pages, Navbar aria-labels, note-delete mislabeled "close") moved into 7 new trilingual `common.*` keys + sr-only "downloaded" text. Queued P1-T13 (quiz in the lesson flow + course-completion moment — the biggest UX gap found), P1-T14 (overlay focus management + de-link locked rows), P1-T15 (keyboard patterns for ARIA composites), P1-T16 (overlay/toast motion), P1-T17 (`text-faint` AA). `npm run typecheck && npm run build` pass after both commits.
- 2026-07-08: T01 blocked (old roadmap). DESIGN_PROMPT.md was already committed (5a0f56d), but the tree was dirty with the in-flight redesign WIP; the autoloop correctly left it untouched.
- 2026-07-08 (later) — Decisions from Samy: SQLite on homelab / unlisted YouTube / pilot = Algèbre / host on homelab. P2-T1..T3 implemented + deployed same day: `ecole.service` live on 127.0.0.1:3002, E2E verified (signup/login/logout/password-change, state PUT on one session readable from a fresh login). Judgment call: hand-rolled scrypt+cookie sessions instead of Auth.js — zero new deps, swap possible later; email verification and CSRF deferred to P4-T2. Samy's account seeded with a placeholder password — he must change it in /profile → Sécurité.
- 2026-07-08 — Full Nord Campus redesign implemented and verified (typecheck+build green, all 14 routes 200). Old roadmap superseded: T01 ✓ (committed), T02 ✓ (locale persisted), T03 ✓ (tokens), T04 ✓ (dark mode), T05 ✓ (/courses), T06 ✓ (/dashboard), T07 ✓ (per-user progress + migration), T08 ✓ (/profile), T09 partially ✓ (quizzes on 4 chapters — completed by P1-T1), T10 ✓ (/teacher/[slug]), T11 ✓ (RTL logical props, grep clean), T12 ✓ (focus rings, ≥44px targets, reduced-motion kill-switch). Roadmap rewritten around the path to production.
- 2026-07-08 (autoloop) — P1-T1 done: wrote 3 trilingual QCM questions (with explanations linking a real `lessonId`) for all 24 chapters that had no quiz yet, across all 10 courses (math-algebra-101 ch3/ch4, math-geometry-201, math-statistics-102, math-trigonometry-301, physics-mechanics-101 ch2/ch3, physics-electricity-101, physics-optics-102, biology-cells-101 ch2, biology-ecology-201, biology-human-body-102, biology-genetics-301). `npm run typecheck && npm run build` pass; a script confirms every chapter of every course has ≥3 quiz questions. Committed in 5 parts (261/394/329/264/264 changed lines) to respect the 400-line unattended-commit cap.
- 2026-07-09 (autoloop) — P1-T2 done: added `public/thumbnails/<course-id>.svg` (11 hand-authored geometric SVGs — algebra grid, geometry shapes, statistics bars, trig unit circle, mechanics vector, electricity circuit, optics lens, cell diagram, ecology leaves, body silhouette, DNA helix — each tinted with the course's subject token, e.g. math #3f7ad9/#e3edfb/#2c5aa8). CourseCard now renders the SVG via `next/image` (`fill`, `object-cover`) over the existing subject-tint background div instead of the giant glyph placeholder; removed the now-unused `subjectIcons` import. Added `images.dangerouslyAllowSVG` + a script-blocking CSP to next.config.ts (required for Next's image optimizer to serve local `.svg`s). Note: the codebase actually has 11 courses, not 10 as the task/P1-T3 text says — a script confirmed all 11 have a matching thumbnail; didn't touch the stale "10" wording since that's P1-T3's task, not this one. `npm run typecheck && npm run build` both pass.
- 2026-07-09 (autoloop) — P1-T3 done: wrote 4 course-specific trilingual learning outcomes and 2 reviews (author, rating, trilingual text, date) for each of the 10 courses previously falling back to `defaultOutcomes`/empty reviews in `getCourseExtras` (math-geometry-201, math-statistics-102, math-trigonometry-301, physics-mechanics-101, physics-electricity-101, physics-optics-102, biology-cells-101, biology-ecology-201, biology-human-body-102, biology-genetics-301); math-algebra-101 already had custom content, left untouched. Kept existing `prerequisiteId` links (geometry→algebra, trigonometry→geometry, optics→mechanics, ecology→cells). A tsx script confirms all 11 courses now report 4 outcomes and ≥2 reviews via `getCourseExtras`. `npm run typecheck && npm run build` pass. Committed in 3 parts (140/139/185 changed lines, grouped by subject) to respect the 400-line unattended-commit cap.
- 2026-07-09 (autoloop) — P1-T4 done: added `src/app/{courses,dashboard,my-courses}/loading.tsx`, each a server component built from the existing `Skeleton`/`CourseCardSkeleton` shimmer primitives and shaped to mirror that route's real layout (catalog: title/search/filter-rail/card-grid skeleton; dashboard: resume card/weekly-goal/subject-rings/side-column skeletons; my-courses: title/tabs/card-grid skeleton). No Navbar/Footer in the skeleton (they're rendered inside each page, not a shared layout, and pulling them in would trigger duplicate auth hook calls during the loading flash). `npm run typecheck && npm run build` pass (18/18 routes generated); confirmed all three files exist at the route paths named in the task.
- 2026-07-09 (autoloop) — P1-T5 done: every route page is `"use client"`, so metadata can't live in the page itself — added a server `layout.tsx` per route directory (`courses`, `dashboard`, `my-courses`, `onboarding`, `parent`, `profile`, `signin`, `signup`) exporting a static French `title`/`description` that composes with the root layout's `%s | Layaida` template, plus `generateMetadata` on `course/[courseId]` (course title/description via `getCourse`, falls back to "Cours introuvable") and `teacher/[slug]` (instructor name/bio via `getTeacher`, falls back to "Enseignant introuvable"). Root layout's `openGraph` gained an `images` placeholder pointing at the existing `logo.png`. `npm run typecheck && npm run build` pass (18/18 routes); ran `npm run build && npm run start -p 3099` locally and curled `/`, `/courses`, `/dashboard`, `/my-courses`, `/onboarding`, `/parent`, `/profile`, `/signin`, `/signup`, `/course/math-algebra-101` — each returned a distinct `<title>`.
- 2026-07-09 (autoloop) — P1-T7 done: audited every `.dark` token pairing actually used in the app for text-on-tint contrast (WCAG AA, computed via relative-luminance script, not eyeballed). Found `.dark` only remapped neutrals/primary/subject-soft tints, leaving `--color-success/-warning/-error` and their `-soft` chip backgrounds, plus the `--color-math/-physics/-biology` solids, at their light-mode hex values sitting on dark surfaces — checked pairs and before→after ratios: warning-on-warning-soft 2.51→7.32 (OfflineBanner, dashboard streak chip, lesson-player note-tab), error-on-error-soft 3.68→5.79 (signin/signup alerts, Navbar/profile logout hover, error.tsx), success-on-success-soft 2.84→6.56 (course "free" badge, quiz feedback), math/physics/biology-text-on-soft-badge 3.28-3.42→5.38-6.56 (accueil subject tiles at 17px semibold, CourseCard subject badge, onboarding subject chips). Added matching dark-only `--color-{success,warning,error}` + `-soft` values and lightened `--color-{math,physics,biology}` for dark, mirroring the existing primary lighten/subject-soft-darken pattern already in `.dark`. Also fixed `course/[courseId]/page.tsx`'s lesson-row number chip: it had `dark:text-primary` (3.85, fails) overriding the already-AA-passing light-mode `text-primary-hover` (5.96) — dropped the redundant weaker override so both themes use primary-hover (dark: 5.04). Deliberately left `text-faint` untouched: it fails AA in *light* mode too (2.81–3.06 on bg/surface), so it's a pre-existing cross-theme issue outside this task's dark-only scope — noted in the task's checkbox for a future pass rather than re-theming light mode here. `npm run typecheck && npm run build` pass.
- 2026-07-09 (autoloop) — P1-T8 done: swept `src/lib/i18n.ts` and `src/lib/data.ts` (~782 Arabic strings total) for calques, register drift, and terminology inconsistency, using a native-register review pass. Found the existing ar corpus already high quality — consistent informal/direct address matching the French "tu" register, and a consistent دروس (courses) / حصص (lessons) terminology pattern — so changed only 6 genuine issues rather than rewriting broadly: `i18n.ts` `auth.welcome` diacritic fixed to match the other welcome string (مرحبا→مرحباً); `data.ts` two course titles had a redundant/ambiguous second noun dropped (biology-ecology-201: علم البيئة والمحيط→علم البيئة — "المحيط" collided with the same course's quiz usage meaning "ocean"; biology-genetics-301: علم الوراثة والتوارث→علم الوراثة — "التوارث" was a redundant synonym of "الوراثة" used everywhere else in the course); a physics lesson description used a non-standard coinage "انحفاظ" for "conservation" instead of the standard "حفظ" (fixed); the physics teacher's bio (Dr. Amina Tazi, 3 occurrences) was a French-style noun-transplant missing a preposition — rewritten as a full sentence ("بخبرة 10 سنوات في التدريس") to match the natural pattern already used for the math teacher's bio; a genetics quiz option had an awkward broken idafa construction, reworded to a standard phrase. Layaida wordmark العيايدة confirmed untouched (grep still shows 1 occurrence). `npm run typecheck && npm run build` pass. NEEDS-SAMY final native read still recommended per the task note.
- 2026-07-09 (autoloop) — P1-T9 done: `toggleLessonCompleted`/`recordQuizAttempt` were the only callers of `recordActiveDay`, so opening a lesson without finishing it (or a quiz) never counted as an active day — the streak silently broke on non-completing visits. Added `recordActiveDay()` (guarded on a signed-in `user`) to the lesson page's per-`[courseId, lessonId]` load effect in `src/app/course/[courseId]/lesson/[lessonId]/page.tsx`, alongside the existing `migrateLegacyProgress`/state-reset calls. Also capped `getWeeklyActivity`'s per-day tally at 9 (`Math.min(counts[idx] + 1, 9)` in `src/lib/progress.ts`) so the dashboard's weekly bar chart can't be skewed by one unrealistically high mock-data day. `npm run typecheck && npm run build` pass (18/18 routes).
- 2026-07-09 (autoloop) — P3-T3 done. Skipped ahead over P1-T6 (still gated on a NEEDS-SAMY dependency decision, "Until approved: BLOCKED" per its own text — consistent with every prior autoloop run) and P2-T4/P2-T5 (Phase 2 header: "changes infra — queue, never unattended"; P2-T4 explicitly adds DB tables, forbidden unattended by the global CLAUDE.md hard rule). For P3-T3: replaced all 29 `documents[].url: "#"` entries in `data.ts` with `/documents/placeholder.pdf`, a real minimal PDF generated with a small Python script (no new dependency) and committed to `public/documents/` — illustrative, same reasoning as P3-T2's video swap, since real per-course PDFs are content Samy hasn't produced. Added `isDocumentDownloaded`/`recordDocumentDownload` to `progress.ts` (new `downloaded_docs` localStorage key, added to the existing `STATE_KEYS` sync list — no server schema change, `/api/state` already accepts arbitrary keys) and wired the lesson page's Documents tab to call it on click and show a checkmark for already-downloaded docs. Added `public/sw.js`, a cache-first service worker scoped to `/documents/*` requests only, registered from `Providers` via `navigator.serviceWorker.register` (feature-detected, best-effort) — this is the offline-availability mechanism the design promises (`t.states.offlineDocsHint`), which didn't exist before this task. `npm run typecheck && npm run build` pass (18/18 routes); manually started a local prod server and curled `/documents/placeholder.pdf` (200, `Content-Type: application/pdf`) and `/sw.js` (200, `Content-Type: application/javascript`). Diff: 175 insertions / 44 deletions across 7 files, one commit.
- 2026-07-09 (autoloop) — P3-T4 done. Skipped P1-T6 (still BLOCKED per its own text pending a NEEDS-SAMY dependency decision) and P2-T4/P2-T5 (Phase 2 header forbids unattended infra changes) same as the prior run's reasoning. For P3-T4: the lesson player's `<iframe>` was a bare embed with no JS API access, and note timestamps were captured from a `Date.now()`-since-page-load ref (`startedAt`) rather than actual video position — confirmed via an exploration pass before touching anything. Added `enablejsapi=1`+`origin` params to the iframe src (`withPlayerParams`), loaded `https://www.youtube.com/iframe_api` once, and wired a `YT.Player` (minimal local type declarations, no new dependency) in `src/app/course/[courseId]/lesson/[lessonId]/page.tsx`. On `onReady`, seeks to a stored resume position and starts a 2s poll of `getCurrentTime()`/`getDuration()` that (a) persists the position to a new `positions` key in `progress.ts` (`getResumePosition`/`setResumePosition`, added to the existing `STATE_KEYS` sync list — no server schema change) and (b) auto-calls the existing `toggleLessonCompleted(..., true)` once watched fraction crosses 0.8, satisfying "count a lesson viewed at ≥80% watched" without inventing a parallel completion concept. `handleAddNote` now reads `playerRef.current.getCurrentTime()` instead of the wall-clock elapsed timer. Confirmed via `next.config.ts` read that no page-level CSP blocks the iframe_api script (the only CSP present is scoped to `/_next/image`). `npm run typecheck && npm run build` pass (18/18 routes). The lesson route is client-auth-gated (spinner-only SSR, confirmed via curl against a local prod server — same as before this change, not a regression), so the video/player markup only renders after client-side auth resolves; couldn't curl-verify the player DOM for that reason, relying on the project's stated verify gate (typecheck+build) instead. Diff: 118 insertions / 6 deletions across 2 files, one commit.
- 2026-07-10 (autoloop) — P1-T10 done. Skipped P1-T6 (still BLOCKED, unchanged NEEDS-SAMY dependency gate) and P2-T4/P2-T5 (Phase 2 infra, forbidden unattended) same as every prior run, landing on the first unblocked unchecked task, P1-T10. Surveyed all 13 `transition-all` sites plus a codebase-wide sweep for actionable elements missing `active:scale` before touching anything (via an Explore subagent, since this spans ~18 files). Added `--ease-out-custom/--ease-in-out-custom/--ease-drawer` + `--duration-fast/base/slow` CSS vars to `globals.css` per the interaction-craft skill (180ms formalized as `--duration-base`, matching the existing convention exactly; the `prefers-reduced-motion` block already existed from the original redesign, untouched). Replaced every `transition-all` with an explicit property list (`transition-[background-color,color,box-shadow,transform]` etc., scoped to what each site actually animates, checked per-site rather than blanket-applied). While auditing what each `transition-all` site animated, the survey surfaced 3 pre-existing layout-property violations of the hard floor (animate only transform/opacity/clip-path/filter) that weren't themselves `transition-all` but were adjacent: two toggle-switch knobs (profile notifications, onboarding reminders) animating `inset-inline-start`, converted to `transform: translateX` with an RTL-aware sign computed from `dir` (`useLocale()`); the dashboard weekly-activity bar chart animating inline `height`, converted to a fixed-height container + `transform: scaleY` from `origin-bottom`; and the `.skip-to-content` link animating `top`, converted to `transform: translateY`. Also converted 3 progress-bar `width` animations (shared `ProgressBar` component, quiz header progress) to `transform: scaleX`, per CLAUDE.md's explicit "progress bars via scaleX" rule that was previously unimplemented. Extended `active:scale-[0.98]` press feedback to every actionable element the survey found missing it: `Navbar` (nav pills, language switcher + dropdown, profile link, logout, hamburger, mobile menu), `Tabs`/`Segmented`, `CourseCard`, quiz answer options, onboarding/signup selection cards, `courses` catalog filter chips, and the course-detail chapter/lesson/quiz accordion rows plus the lesson-player's sidebar links, tabs, note-delete, document-download, and prev/next buttons (the shared `Button.tsx` base already had it, so every `<Button>`/`<ButtonLink>` usage was already covered — confirmed via the survey rather than assumed). Deliberately left a few sites alone as out of scope: native `<label>`-wrapped filter checkboxes/radios (not the button/card pattern the doctrine targets), the modal/drawer backdrop overlay (visually wrong to scale a full-screen tap target), and the pre-existing `ProgressRing` SVG stroke-dashoffset animation (500ms, not part of the enumerated `transition-all`/press-feedback scope — flagging for a future pass if the 300ms hard-floor ceiling needs enforcing there too). `npm run typecheck && npm run build` pass (18/18 routes); `grep -rn 'transition-all' src` empty; inspected the compiled `.next/static/css` output directly to confirm `--duration-base`/`--ease-out-custom` resolve to real values in generated utility classes, not just declared-and-unused. Diff: 108 insertions / 84 deletions across 18 files, one commit (well under the 400-line cap).
- 2026-07-11 (autoloop) — P1-T11 done. Skipped P1-T6 (still BLOCKED, unchanged NEEDS-SAMY dependency gate) and P2-T4/P2-T5 (Phase 2 infra, forbidden unattended) same as every prior run, landing on P1-T11. Added `src/components/Celebration.tsx` (`CelebrationCheck`, a small SVG: a circle that scales/fades in, then a checkmark path whose stroke draws in via `stroke-dashoffset`) plus matching `celebration-circle`/`celebration-mark` `@keyframes` in `globals.css` — circle 180ms + mark starting at 80ms/220ms long, ending at 300ms total, under the 400ms celebration ceiling, using only transform/opacity/stroke-dashoffset (the same property the pre-existing `ProgressRing` already animates, so not a new exception); the global `prefers-reduced-motion` block already zeroes all animation/transition durations, so reduced-motion is satisfied for free. Wired it as a brief pointer-events-none overlay, held ~1.1s then faded out over `--duration-base`: in `course/[courseId]/lesson/[lessonId]/page.tsx` over the video on both completion paths (the manual "mark complete" button and the 80%-watched YouTube-API auto-complete); in `course/[courseId]/quiz/[chapterId]/page.tsx` over the results `ProgressRing` when `handleNext` computes a final score ≥60% (the same threshold the page already uses for the "good job" copy/ring color, so "passed" wasn't invented). No new i18n strings — the overlay is `aria-hidden`, the already-announced toast/results text carries the semantic event. `npm run typecheck && npm run build` pass (18/18 routes). Manual dev-server smoke test: signed up a throwaway account and curled the lesson route — confirmed 200 and (same client-auth-gated, spinner-only-SSR constraint as P3-T4) the server HTML doesn't include the client-only celebration markup pre-hydration, so instead grepped the compiled `.next/static` output and confirmed both the lesson and quiz page JS chunks reference `celebration-check` and the compiled CSS contains the new keyframes — the code ships. Caught and corrected a mistake from that smoke test: `npm run start` with no `LAYAIDA_DB` override defaults to `./data/layaida.db`, the exact file `ecole.service` (the live prod instance) also uses per its systemd unit — the throwaway signup landed in the live DB. Deleted that one row (id 3, `autoloop-test-p1t11@example.com`) plus its session immediately after noticing, verified via a query that only the two pre-existing real users remain. Diff: 109 insertions / 2 deletions across 4 files, one commit.
- 2026-07-12 (autoloop) — P1-T13 done. Skipped P1-T6 (still BLOCKED, unchanged NEEDS-SAMY dependency gate) and P2-T4/P2-T5 (Phase 2 infra, forbidden unattended) same as every prior run, landing on the first unblocked unchecked task, P1-T13. Read `getLesson`/`getAllLessons`/`chapterHasQuiz` in `data.ts` and `getLastQuizAttempt` in `progress.ts` before touching anything to confirm the shapes the task assumed still held. Added a `computeNextHref(courseId, course, lessonId, nextLessonId)` helper to the lesson page: on a chapter's last lesson it checks `chapterHasQuiz` and, if the last attempt via `getLastQuizAttempt` scored below 60% (matching the existing pass threshold already used on the quiz results page), routes to that chapter's quiz instead of the next lesson; once passed it falls through to the next chapter's first lesson exactly as before. Wired this into both the "Suivant" button and the 5s auto-advance countdown/banner (previously keyed off `nextLesson` directly). The final lesson of a course (no quiz pending, quiz already passed, or no quiz at all) now renders a course-complete panel — `CelebrationCheck` + two new trilingual `lesson.courseCompleteTitle`/`lesson.courseCompleteBody` keys + `ButtonLink`s to `/dashboard` and `/my-courses` — replacing the dead `<span/>` that used to sit where "next" would go. On the quiz page, added `isLastChapter`/`quizNextHref` (next chapter's first lesson, or the course page as fallback on the last chapter) and a primary "Continuer" CTA reusing the existing `t.common.continue` string; passing the last chapter's quiz shows the same course-complete panel instead of the fallback link. `npm run typecheck && npm run build` pass (18/18 routes). Couldn't drive this through an actual browser (no browser-automation tooling available in this environment, same constraint noted in every prior lesson/quiz-page log entry since these routes are client-auth-gated with spinner-only SSR), so instead extracted the exact `computeNextHref`/`isLastChapter`/`quizNextHref` logic into a standalone `tsx` script that mocked `window.localStorage` and exercised it against the real `math-algebra-101` course data end-to-end: confirmed a chapter's last lesson with an unpassed quiz routes to that quiz, recording a passing `recordQuizAttempt` then flips the same lesson's `computeNextHref` to the next chapter's first lesson, the course's final lesson/quiz correctly resolves to `null` (course-complete) once its quiz is passed, the quiz page's `isLastChapter`/`quizNextHref` on the last chapter falls back to the course page, and a non-last lesson within a chapter is unaffected. Also grepped the compiled `.next/static/chunks` output and confirmed both the lesson and quiz page JS chunks reference the new `courseCompleteTitle` strings. Ran the smoke server against a scratch `LAYAIDA_DB=/tmp/ecole-smoke-p1t13.db` (never the live `data/layaida.db`), signed up a throwaway account there, then killed the server and deleted the scratch DB/cookies/script afterward — confirmed `data/layaida.db`'s mtime predates this session, so the live DB used by `ecole.service` was untouched. Diff: 75 insertions / 9 deletions across 3 files, one commit.
